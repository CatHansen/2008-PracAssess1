const itemDescription = function (title="itemDescription", className='itemDescription'){

    const element = `<p class="${className}">${title}</p>`

    return element

}

export default itemDescription